<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 桌面小部件「记一笔」的悬浮速记条（桌面直接打字，不开 App） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 微信/QQ 跳转需要包可见性声明（Android 11+） -->
    <queries>
        <package android:name="com.tencent.mm" />
        <package android:name="com.tencent.mobileqq" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="路远"
        android:supportsRtl="true"
        android:theme="@style/Theme.Luyuan"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="路远"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Luyuan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 系统分享接收：任意 App「分享 → 路远」一键存成记事（透明无界面） -->
        <activity
            android:name=".ShareActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- 图片分享接收（v1.13）：相册/聊天图片 → 分享 → 路远，压缩进 images/ 带配图落库 -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <service
            android:name=".platform.LuyuanService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- 桌面小部件「记一笔」的悬浮速记条 -->
        <service
            android:name=".platform.FloatingQuickNoteService"
            android:exported="false" />

        <!-- 通知自动记账：监听微信/支付宝支付通知 → 待确认（用户点 ✓ 才真入账） -->
        <service
            android:name=".platform.PaymentNotificationListener"
            android:exported="false"
            android:label="路远记账通知监听"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".platform.TodoActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.luyuan.action.TODO_DONE" />
                <action android:name="com.luyuan.action.TODO_DROP" />
            </intent-filter>
        </receiver>
        <!-- 消息待办：监听微信/QQ 聊天通知 → 粗筛 → 云端抽取 → 待确认（默认关闭，开关在设置页） -->
        <service
            android:name=".platform.MessageNotificationListener"
            android:exported="false"
            android:label="路远消息待办监听"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".platform.QuickRecordTile"
            android:exported="true"
            android:icon="@drawable/ic_tile_mic"
            android:label="路远录音"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".platform.ReminderReceiver"
            android:exported="false" />

        <!-- 桌面小部件：记一笔 / 说话（AppWidget 官方样例为 exported=true：系统桌面需跨 UID 下发更新广播） -->
        <receiver
            android:name=".platform.QuickWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_widget_info" />
        </receiver>

        <!-- 桌面小部件：今日卡（课程/待办/支出/最近，深链各页） -->
        <receiver
            android:name=".platform.TodayWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_widget_info" />
        </receiver>

        <receiver
            android:name=".platform.JournalReceiver"
            android:exported="false" />

        <receiver
            android:name=".platform.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".platform.VolumeKeyService"
            android:exported="true"
            android:label="路远·双击音量下键录音"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/volume_service_config" />
        </service>
    </application>
</manifest>
